# 📰 新闻数据收集系统 (升级版API - 高速模式)

本系统用于收集NASDAQ、S&P 500、NYSE三个市场的股票新闻数据和情绪分析。

## ⚡ 升级版API优化特性

- **无限制调用**: 升级版API支持无频率限制
- **高速收集**: 大幅减少等待时间和间隔
- **大批次处理**: 批次大小从50增加到200
- **快速间隔**: API请求间隔从0.3秒减少到0.05秒
- **短休息时间**: 批次间休息从10秒减少到1秒

## 🚀 快速开始

### 1. 安装依赖包
```bash
cd src
pip install -r requirements_sentiment.txt
python -c "import nltk; nltk.download('punkt')"
```

### 2. 运行统一管理脚本 (推荐)
```bash
python run_all_news_collection.py
```

### 3. 或者单独运行各市场脚本
```bash
# 收集NASDAQ新闻数据 (高速模式)
python prepare_nasdaq_news_data.py

# 收集S&P 500新闻数据 (高速模式)
python prepare_sp500_news_data.py

# 收集NYSE新闻数据 (高速模式)
python prepare_nyse_news_data.py
```

## 📊 数据结构

收集完成后，数据将保存在以下目录结构中：

```
dataset/
├── NASDAQ/
│   └── news_sentiment/
│       ├── {TICKER}_news_detailed.csv        # 每只股票的详细新闻数据
│       ├── {TICKER}_daily_sentiment.csv      # 每只股票的日度情绪数据
│       ├── nasdaq_combined_daily_sentiment.csv  # 合并的日度情绪数据
│       └── nasdaq_news_tickers.txt           # 成功收集数据的股票列表
├── SP500/
│   └── news_sentiment/
│       ├── {TICKER}_news_detailed.csv
│       ├── {TICKER}_daily_sentiment.csv
│       ├── sp500_combined_daily_sentiment.csv
│       └── sp500_news_tickers.txt
└── NYSE/
    └── news_sentiment/
        ├── {TICKER}_news_detailed.csv
        ├── {TICKER}_daily_sentiment.csv
        ├── nyse_combined_daily_sentiment.csv
        └── nyse_news_tickers.txt
```

## 📈 情绪分析特征

每条新闻都会提取以下特征：

### 基础信息
- `title`: 新闻标题
- `description`: 新闻描述
- `published_utc`: 发布时间
- `article_url`: 新闻链接
- `publisher`: 发布商

### 情绪分析
- `sentiment_polarity`: 情绪极性 (-1到1，负面到正面)
- `sentiment_subjectivity`: 主观性 (0到1，客观到主观)
- `sentiment_label`: 情绪标签 (positive/negative/neutral)
- `sentiment_confidence`: 情绪置信度

### 关键词检测
- `keywords_earnings`: 财报相关
- `keywords_acquisition`: 并购相关  
- `keywords_regulation`: 监管相关
- `keywords_partnership`: 合作相关
- `keywords_product`: 产品相关
- `keywords_financial`: 金融相关
- `keywords_leadership`: 管理层相关
- `keywords_lawsuit`: 诉讼相关

### 其他特征
- `urgency_score`: 紧急程度评分
- `text_length`: 文本长度
- `ticker`: 股票代码

## 📋 日度聚合数据

日度情绪数据包含以下聚合特征：

- `sentiment_polarity_mean`: 当日平均情绪极性
- `sentiment_polarity_std`: 当日情绪极性标准差
- `sentiment_polarity_count`: 当日新闻数量
- `sentiment_confidence_mean`: 当日平均情绪置信度
- `urgency_score_mean`: 当日平均紧急程度
- `keywords_*_sum`: 各类关键词当日出现次数
- `text_length_mean`: 当日平均文本长度

## ⚙️ 升级版API配置说明

### API配置
- 使用Polygon.io升级版API
- **无限制调用** (无频率限制)
- 时间范围：2020-01-01 到 2024-12-31

### 高速收集参数
- **批次大小**: 200只股票/批 (原50只，提升4倍)
- **API请求间隔**: 0.05秒 (原0.3秒，提升6倍速度)
- **批次间休息**: 1秒 (原10秒，提升10倍速度)
- **市场间等待**: 5秒 (原30秒，提升6倍速度)
- **单只股票新闻数量限制**: 1000条

### 股票数量
- **NASDAQ**: 952只股票 (数据集中所有股票)
- **S&P 500**: 502只股票 (数据集中所有股票)
- **NYSE**: 1619只股票 (数据集中所有股票)
- **总计**: 3073只股票的新闻情绪数据

## 🎯 使用建议

1. **完整模式**: 升级版API支持直接运行完整模式收集所有数据
2. **高速收集**: 利用升级版API的高速优化，大幅减少收集时间
3. **连续收集**: 建议一次性收集所有三个市场的数据
4. **数据检查**: 使用统一管理脚本的状态检查功能验证收集完整性

## 🔧 故障排除

### 常见问题
1. **API错误**: 检查网络连接和API密钥
2. **依赖包错误**: 运行 `pip install -r requirements_sentiment.txt`
3. **NLTK数据**: 确保运行了 `nltk.download('punkt')`
4. **权限错误**: 确保有写入权限到dataset目录

### 重新收集
如果某个市场收集失败，可以单独重新运行对应的脚本，系统会自动跳过已存在的文件。

## 📅 后续集成

数据收集完成后，可以通过以下方式集成到StockPredict模型中：

1. 将情绪特征与价格数据对齐
2. 修改训练脚本集成多模态特征
3. 使用情绪分析增强模型预测能力

## ⚡ 性能提升总结

相比原版本，升级版API优化后的性能提升：

- **收集速度**: 提升约10-15倍
- **批次处理**: 提升4倍效率 (50→200股票/批)
- **API间隔**: 提升6倍速度 (0.3s→0.05s)
- **整体时间**: 预计总收集时间从数小时缩短到30-60分钟

---

**注意**: 此系统专为升级版API设计，享有无限制调用权限和高速优化配置。如遇到任何问题，请检查API密钥和网络连接。 